query GetLiveEventByAccessToken($token: String!) {
  getLiveEventByAccessToken(accessToken: $token) {
    id
    name
    accessToken
    description
    interactions {
      id
      name
      text
      type
    }
  }
}
